# CPU-only torch is installed from its official CPU wheel index by build-windows.ps1;
# the floor below is what the dependency scanner resolves against.
torch>=2.10.0
PySide6>=6.8,<6.11
PyInstaller>=6.12,<7
schift-ko-pii==0.6.0
transformers==5.15.0
safetensors>=0.5,<1
huggingface-hub>=1.5,<2
defusedxml>=0.7.1
# Transitive via requests/huggingface-hub; 3.15 fixes GHSA-65pc-fj4g-8rjx.
idna>=3.15
